server:
    port: 8081

spring:
    datasource:
        password: root
        url: jdbc:mysql://localhost:3306/xiaoxin_notes?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
        username: root

    data:
        elasticsearch:
            repositories:
                enabled: true
            cluster-nodes: 127.0.0.1:9200
            cluster-name: elasticsearch


mongodb:
    host: localhost # mongodb的连接地址
    port: 27017 # mongodb的连接端口号
    database: notes # mongodb的连接的数据库
